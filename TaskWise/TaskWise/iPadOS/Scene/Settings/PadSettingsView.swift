import SwiftUI

struct PadSettingsView: View {
    var body: some View {
        VStack {
            Text("Hello, iPad!")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color.appBackground)
    }
}

#Preview {
    PadSettingsView()
}
